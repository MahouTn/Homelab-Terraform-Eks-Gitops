pipeline {
    environment {
        IMAGE_FULL_URI = '802617578034.dkr.ecr.eu-west-1.amazonaws.com/homelab-eks:latest'
    }

    agent {
        kubernetes {
            yaml '''
kind: Pod
metadata:
  name: kaniko-builder
  namespace: ci-cd
spec:
  serviceAccountName: kaniko-sa
  containers:
  - name: kaniko
    image: gcr.io/kaniko-project/executor:a946b82f22240eb8e3f7e73aaf0e592a323fa466-debug
    command:
      - /busybox/sh
      - -c
      - "tail -f /dev/null"
    tty: true
'''
            defaultContainer 'kaniko'
        }
    }

    stages {
        stage('Build and Push Image') {
            steps {
                container('kaniko') {
                    sh """
                    /kaniko/executor \
                        --context=${WORKSPACE}/app-code/nodejs-demoapp/build \
                        --dockerfile=./Dockerfile \
                        --destination=${IMAGE_FULL_URI}
                    """
                }
            }
        }
    }
}
